Turn 11 — gap-analyst: "Be blunt about discrepancies — your Learnings section is what makes the requirements improve each turn." → added a rule to re-verify inherited multi-turn gaps against the current file before repeating them, and to promote any gap recurring 3+ turns straight to `next` instead of re-flagging it as a footnote — evidence: evolution/weaknesses.md's architect/gap-analyst sections (mined turn 10 from log.md turns 4-9) plus this turn's direct re-read of architecture.md showing the "stale no-dependencies line" gap was already resolved/mis-scoped, while the real `ui-tester` gap had been re-flagged 5+ times without ever being set as `next` until turn 10.
Turn 11 — ui-tester: "Then perform an exploratory walkthrough with the Playwright MCP browser tools... take a screenshot at every step..." → same + "If the MCP browser fails to launch (e.g. no system Chrome and no sudo to install it), do not skip the walkthrough — drive the same steps with a short script using the project's own installed Playwright browser instead, and note the substitution plainly in the report." — the MCP browser could not launch this run (no system Chrome, no sudo); without this fallback documented, future runs might skip the walkthrough or stall instead of substituting a script-driven walkthrough with the project's own installed browser — evidence: mcp__playwright__browser_navigate error this run.
Turn 11 — developer: "Run the app or a quick check via Bash to confirm your change actually works before finishing." → "...and if 05-qa/ already has an automated test suite, run it too — a UI change that looks fine standalone can silently break existing DnD/locator-based tests through layout/hit-testing side effects." — task-010's new "Move to..." select silently broke 2 existing Playwright DnD tests via a hit-testing/layout collision; a manual smoke check wouldn't have caught it — evidence: 05-qa suite went 7/7 -> 5/7 then back to 7/7 after the fix.
Turn 11 — ui-tester (bug-001 re-verification, no new definition edit): probation from this same turn's earlier ui-tester run verdict KEPT — MCP browser again failed to launch (same missing-system-Chrome/no-sudo error) and the documented script-based fallback again worked cleanly, this time also revealing that `locator.dragTo()` is unreliable for the narrow list-header DnD case (fixed in the test via manual `mouse.move()` steps, not an application fix) — evidence: mcp__playwright__browser_navigate error this run; journeys.spec.js list-reorder test failing 2/2 with dragTo then passing 10/10 after switching to manual mouse events.
