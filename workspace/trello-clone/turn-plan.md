# Turn Plan — trello-clone (assessed turn 13)

## State

- `idea.md`: "analog of trello boards" — one line, unchanged since turn 1.
- **Requirements** (`01-understand/requirements.md`): FR1-FR14 + NFR1-NFR7, `review.md` — VERDICT: APPROVED. All 8 Must FRs and all 3 Should FRs are built, reviewed, and UI-verified (turn 11). **NFR7 (visible warning on persistence-save failure) remains specified but unbuilt** — flagged as a gap since turn 12, still true this turn: no `task-017` file exists in `03-plan/tasks/`, `03-plan/backlog.md` has no row for it, and `storage.js` still only `console.warn`s on save failure with no UI-facing signal.
- **Design** (`02-design/`): architecture.md, api.md, data-model.md — APPROVED turn 1, no change needed.
- **Plan** (`03-plan/backlog.md`, 16 rows): 13/16 DONE (all Must/Should scope). task-012/013/014 (Could-have: label, due date, search) remain TODO by design — deliberately deprioritized behind ship-readiness per turn 12's plan. No row exists yet for NFR7.
- **Build** (`04-build/src/`): full CRUD for boards/lists/cards, cascading deletes, confirm-before-delete, card detail modal, and drag-and-drop (cards/lists/boards) plus FR6a click-fallback — all reviewed VERDICT: APPROVED. Unchanged since turn 11; turn 12 added no build work (docs/security only).
- **QA** (`05-qa/`): Playwright suite at 10 specs, all passing; `ui-tester` ran twice (turn 11), zero open bugs (bug-001 resolved). No test exists yet for the NFR7 failure path (not built) — expected, since the feature itself doesn't exist.
- **Ship** (`06-ship/`): `user-docs.md` and `security-review.md` now exist (both written turn 12). `security-review.md` — **VERDICT: APPROVED**, no exploitable findings; XSS mitigation, path-traversal guard, and no-secrets/no-deps all confirmed clean. Its one informational item worth carrying forward: `api.md` documents `setCardLabel`/`setCardDueDate`/`filterCards` (FR12-14) that `store.js` doesn't implement yet — a docs/build drift that will self-resolve if/when task-012-014 are ever built, or should be noted as "not yet implemented" in `api.md` if they're formally deferred indefinitely. **`release-notes.md` is still missing** — the last piece of `06-ship/` needed before a legitimate `release-manager` GO.
- **Reflections/evolution**: turn 11's three self-reflection edits (`gap-analyst`, `ui-tester`, `developer`) are logged; `gap-analyst`'s and `ui-tester`'s were validated **KEPT** in turn 12. `developer`'s probation verdict is still pending — no `developer` run has occurred since turn 11 to judge it against. Turn 13 is not a multiple of 5, so no `harness-miner` run is due.
- `state.json`: turn 12, last_action `security-reviewer`, next `release-manager` — this pointer is premature: turn 12's own turn-plan explicitly called for a small `developer:task-017` (NFR7) before reassessing for `release-manager`, and that step was skipped this cycle (turn 12's Done log says so directly: "did not build `developer:task-017`... that remains open for the next turn").

## Learnings

1. **Turn 12 executed two of its own three recommended actions but silently dropped the third.** `turn-plan.md` (assessed turn 12) explicitly said "`tech-writer`... `security-reviewer`... followed by a small `developer:task-017` for NFR7 — then reassess for `release-manager`." Only the first two ran; `state.json.next` was set straight to `release-manager` anyway, skipping the NFR7 step its own plan called mandatory. This is a real process gap worth naming: a turn's own stated plan should gate what `next` gets set to, not just what got done.
2. **NFR7 has now gone unbuilt for 12 consecutive turns** despite being specified in the original requirements (turn 1) and re-flagged as a planning risk in `backlog.md`'s Risks section since task-002. It is small (a save-failure UI banner) and cheap relative to how long it has been carried as an open item — there is no longer a good reason to defer it past this turn.
3. **The project is otherwise release-ready.** Every Must/Should FR built and independently UI-verified, zero open bugs, security review clean, user docs written. The only two things standing between here and a legitimate `release-manager` GO are: (a) closing NFR7 (or explicitly descoping it via `product-owner` if the team decides it's not worth building), and (b) writing `release-notes.md`. Both are small compared to the 12 turns of feature work already banked.

## Gaps (prioritized)

1. **NFR7 still untracked and unbuilt** — no `task-017`, no backlog row, no code. This was called out as the plan for turn 13 in the prior turn-plan and did not happen; it is the single remaining functional gap before release. Options: build it now (small task: `storage.js` failure detection + a non-blocking UI warning banner), or have `product-owner` formally descope it if the team judges the risk (private-browsing/quota-exceeded silent data loss) acceptable for a local single-user tool — but a decision must be made, not silently skipped again.
2. **`release-notes.md` missing** — `06-ship/` is otherwise populated (`user-docs.md`, `security-review.md` both approved) but this file doesn't exist yet; needed before `release-manager` can plausibly issue a GO.
3. **`api.md`/`store.js` drift on FR12-14** (informational, from `security-review.md` item 5) — cosmetic, not blocking, but should be resolved one way or another (build the Could-haves, or mark them "planned, not yet implemented" in `api.md`) before calling documentation fully accurate.
4. **Could-have features (task-012/013/014) unbuilt** — lowest priority, correctly deferred; no change to this assessment.
5. **`developer`'s turn-11 probation edit still unresolved** — needs a `developer` run to judge the "run the existing test suite before finishing" addition against a live case.

## Next

**Recommendation: `developer:task-017` (build NFR7 — persistence-save-failure warning), then reassess for `release-manager`.**

Rationale: this is exactly what turn 12's own plan called for and did not execute — the single remaining functional/spec gap in an otherwise feature-complete, verified, documented, and security-reviewed project. It is small (S-sized: detect `storage.save()` failure, surface a non-blocking UI banner), has no dependency on anything unbuilt, and closes a gap that has been carried for 12 turns. Once task-017 lands (with its `code-reviewer` gate) and `06-ship/release-notes.md` is written, the project will have nothing legitimate left to block a `release-manager` GO. Do not jump straight to `release-manager` this turn without first resolving NFR7 — doing so again would repeat the exact process gap this turn-plan is flagging.
