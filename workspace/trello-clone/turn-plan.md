# Turn Plan — trello-clone (assessed turn 12)

## State

- `idea.md`: "analog of trello boards" — one line, unchanged since turn 1.
- **Requirements** (`01-understand/requirements.md`): FR1-FR14 + NFR1-NFR7, `review.md` — VERDICT: APPROVED. All 8 Must FRs (FR1-FR8, incl. FR6a) and all 3 Should FRs (FR9-FR11) are built, reviewed, and now UI-verified. NFR7 (visible warning on persistence failure) is specified but has **no backlog task and no implementation** — a genuine untracked gap, now flagged for the second turn.
- **Design** (`02-design/`): architecture.md, api.md, data-model.md — APPROVED turn 1. Turn 11's re-read confirmed the long-running "stale no-dependencies line" flag (turns 4-10) was a false alarm; nothing further to do here.
- **Plan** (`03-plan/backlog.md`, 16 tasks): **13 of 16 DONE.** Turn 11 closed task-009 (list DnD), task-016 (board DnD), and task-010 (click-fallback) — the last Must/Should-scope items. Remaining: task-012 (card label), task-013 (due date), task-014 (search filter), all Could-have, all TODO. No task exists yet for NFR7.
- **Build** (`04-build/src/`): full CRUD for boards/lists/cards with cascading deletes, confirm-before-delete on all three paths, card detail modal (Save/Cancel), and drag-and-drop for cards/lists/boards plus the FR6a click-fallback. All 13 completed tasks reviewed VERDICT: APPROVED, first pass, zero bounces.
- **QA** (`05-qa/`): Playwright suite now 10 specs, all passing (`test-report.md`). `ui-tester` ran twice in turn 11: first pass filed `bug-001` (list/board DnD + FR6a fallback missing from the UI despite store-layer support); developers fixed it same turn; second `ui-tester` pass re-verified and marked bug-001 **RESOLVED** with 23 total walkthrough screenshots (`05-qa/walkthrough/turn-011/`). **No open bugs remain.** No dedicated test exists yet for the NFR7 failure path (not built) or for the Could-have surface (not built).
- **Ship** (`06-ship/`): **completely empty** — no `user-docs.md`, no `security-review.md`, no `release-notes.md` exist. This is now the largest untouched shelf in the project, and correctly so up to this point (Must/Should scope wasn't done until turn 11), but there is no longer a reason to defer it.
- **Reflections/evolution**: turn 11 recorded three self-reflection edits (`gap-analyst`, `ui-tester`, `developer`), all evidence-cited and logged with `[PROBATION]` in `evolution/ledger.md`; `ui-tester`'s edit was already re-validated **KEPT** within the same turn. `developer`'s edit verdict is still pending its next run. Turn 12 is not a multiple of 5, so no `harness-miner` run is due.
- `state.json`: turn 11, last_action `ui-tester`, next `developer:task-012` — this next-action pointer is stale relative to reality: Must/Should scope is fully done and verified, so the highest-value action is no longer another feature task but closing the NFR7 gap and populating `06-ship/`.

## Learnings

1. **NFR7 was specified in turn 1 and has never been translated into a task, across 11 turns.** `03-plan/backlog.md`'s own Risks section flagged the underlying UX decision as far back as task-002/task-007 planning ("worth a UX decision before task-007+ pile more state on top") and it was never resolved into backlog work. This is not a documentation nicety — it's a real requirement (`storage.js` only `console.warn`s on save failure today) with zero build evidence. `state.json.next` pointing at Could-have `task-012` while a Must-adjacent NFR sits completely unbuilt is a planning miss worth naming directly.
2. **Feature-complete ≠ ready to ship.** All Must/Should FRs are done, reviewed, and now UI-verified with zero open bugs (bug-001 resolved turn 11) — a genuinely strong state. But `06-ship/` has never been touched: no user docs, no security review, no release notes. Continuing straight to Could-have features (task-012-014) before addressing this would mean shipping polish nobody can read about, on a codebase nobody has looked at through a security lens (e.g., render.js's user-input escaping into innerHTML is claimed correct by code reviewers but has never had a dedicated adversarial pass).
3. **`state.json.next: developer:task-012` is the wrong default action this turn.** It reflects the backlog's raw next-row-in-order (Could-have labels) rather than the actual highest-value gap (NFR7 + ship-readiness). Backlog ordering is a sequencing aid, not an override of turn-by-turn value judgment — this turn-plan should call that out explicitly rather than rubber-stamp the pointer.

## Gaps (prioritized)

1. **NFR7 untracked and unbuilt** — no task exists for the persistence-failure warning despite being specified since turn 1 and flagged as a planning risk since task-002. Needs a small task (e.g. task-017: `storage.js` failure detection + a non-blocking UI warning banner) before the project can be called release-ready.
2. **`06-ship/` entirely empty** — no user-docs.md, no security-review.md, no release-notes.md. Blocks any legitimate `release-manager` GO; this is now the single largest gap in the project by shelf-completeness.
3. **No dedicated security pass** — user input (card/list/board titles, card descriptions) flows into rendered HTML; code reviewers have repeatedly noted "XSS-safe escaping" by inspection but no `security-reviewer` has ever independently verified this claim.
4. **Could-have features unbuilt** (task-012/013/014) — real, but genuinely lowest priority now: they don't block release per the requirements' own MoSCoW, and doc/security work is cheaper to do before, not after, more surface area is added.
5. **Thin failure-path test coverage** — once NFR7 is built, it needs its own automated test (simulate storage quota/unavailability); not urgent until the feature exists.

## Next

**Recommendation: `tech-writer`, in parallel with `security-reviewer`, followed by a small `developer:task-017` for NFR7 — then reassess for `release-manager`.**

Rationale: every Must and Should functional requirement is now built, reviewed, and independently UI-verified with zero open bugs — the feature-complete core reached in turn 11 is solid (13/13 tasks approved first-pass, bug-001 fully closed). Continuing down the backlog into Could-have features (`task-012`, the current `state.json.next`) is the lowest-value action available: it adds optional scope while the project's actual biggest gap — a completely empty `06-ship/` shelf and an untracked NFR7 requirement — sits unaddressed. `tech-writer` and `security-reviewer` have no dependency on each other or on further building, so they should run in parallel per the parallelism rule; a small NFR7 task should follow immediately after since it's the last real spec-vs-build gap standing between this project and a legitimate `release-manager` review. Do not build task-012/013/014 this turn.
