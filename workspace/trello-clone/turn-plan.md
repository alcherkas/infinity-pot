# Turn Plan — trello-clone (assessed turn 11)

## State

- `idea.md`: "analog of trello boards" — one line, unchanged since turn 1.
- **Requirements** (`01-understand/requirements.md`): FR1-FR14 + NFR1-NFR7, `review.md` — VERDICT: APPROVED. FR8 was reconciled turn 10 to match the shipped Save/Cancel modal (task-007) — this closed a real spec-vs-reality drift; text now correctly matches the app.
- **Design** (`02-design/architecture.md`, `api.md`, `data-model.md`): `review.md` — VERDICT: APPROVED (turn 1, no bounce). Re-reading `architecture.md` this turn line-by-line: the "no dependencies" claims are actually scoped correctly — line 36 says the *static server* has no dependencies (true, it uses only Node's built-in `http`), and the Tech Stack table (lines 79-87) explicitly lists "Playwright for QA" as a dependency with justification, plus "Running Locally" (line 95) clarifies "zero npm dependencies for the app itself." There is no literal stale/contradictory sentence left to fix. Nine consecutive turns of gap-analyst flagging this appear to have been chasing a resolved or non-existent problem — see Learnings.
- **Plan** (`03-plan/backlog.md`, 16 tasks): 9 of 16 DONE (task-001 through 007, task-011, task-015, task-008). 6 remain: task-010 (click-move fallback), task-009 (list DnD), task-016 (board DnD), task-012/013/014 (Could-have: labels, due dates, search).
- **Build** (`04-build/src/`): boards/list/card CRUD with cascade deletes, card detail modal (Save/Cancel), confirm-before-delete on all three delete paths, and now card drag-and-drop (`reorderCard`, same-list + cross-list, task-008, turn 10). Every task through task-008 reviewed APPROVED first-pass — 10 tasks, zero bounces. List DnD (FR6), board DnD (FR9), and the click-fallback (FR6a) are NOT YET BUILT. NFR7 (visible warning on persistence failure) remains unimplemented since task-002 (turn 1) — `storage.js` only `console.warn`s.
- **QA** (`05-qa/`): Playwright harness built turn 4, run once turn 7 (1/1 passing, boards-overview only). No bugs filed (`05-qa/bugs/` still empty). Coverage remains thin — zero automated coverage for lists, cards, cascades, the card modal, confirm dialogs, or the new card DnD (task-008, turn 10, unverified since landing). `ui-tester` has **still never run, now 10 turns** despite a real, now drag-and-drop-capable interactive UI existing since turn 3. `state.json.next` has pointed at `ui-tester` since turn 10 but it has not yet actually been launched.
- **Ship** (`06-ship/`): empty. Correctly premature — list/board DnD (Must/Should FRs) still unbuilt.
- **Reflections/evolution**: `reflections.md` still empty across all 10 turns — no `CHANGES_REQUESTED` cycle has occurred. `evolution/ledger.md` remains header-only. `evolution/weaknesses.md` (rewritten turn 10 by `harness-miner`) has two sections directly relevant here: `architect` (told to reconcile the "no dependencies" line) and `gap-analyst` (told to promote a 3+-turn-recurring gap straight to `next` instead of re-flagging it as a footnote). Both cite this project's own log lines.
- `state.json`: turn 10, last_action `code-reviewer:task-008`, next `ui-tester` — consistent with backlog and log.

## Learnings (built reality vs. requirements/process — this is where this turn's assessment differs from turn 10's)

1. **The "stale no-dependencies line" gap that `gap-analyst` re-flagged for nine consecutive turns (4,5,6,8,9,10) does not actually exist as described.** Reading `architecture.md` in full (not just line 36) shows the doc already correctly scopes the claim to "the static server" and separately documents Playwright as an explicit, justified QA dependency in three different places (Tech Stack table, "Running Locally" section, and the original turn-4 review note). This is a real process failure worth naming plainly: `gap-analyst` kept citing an isolated sentence out of context instead of checking whether the surrounding document already resolved it, and `evolution/weaknesses.md`'s own `architect` section (mined turn 10) is itself likely mis-diagnosing a non-bug. This turn-plan is closing that loop by verifying the actual file rather than repeating the inherited claim — future `gap-analyst` runs should always re-read the cited passage in full before repeating a multi-turn-old flag, not just trust the log's characterization of it.
2. **`ui-tester` has been the stated `next` action for a full turn (turn 10 → turn 11) without being launched.** This is the single highest-value, most overdue action in the project: 10 turns, a full CRUD + modal + confirm-dialog + drag-and-drop UI, and zero exploratory human-shaped verification. The Playwright smoke test only covers the boards-overview surface (create/persist), so DnD (task-008, landed last turn) and the modal (task-007) have **never been exercised by anything except static code review.** `evolution/weaknesses.md`'s `gap-analyst` lesson — promote a 3+-turn gap to `next` directly — applies exactly here, and this turn finally does that.
3. **NFR7 remains a silent, unaddressed gap since turn 1** (`storage.js` only `console.warn`s on save failure, no UI-visible warning) — never yet assigned to any task in `backlog.md`. It should get a task slot (small, additive) rather than continuing to ride along as a footnote.

## Gaps (prioritized)

1. **`ui-tester` has never run.** Highest-priority action — exercise the full built surface (boards, lists, cards, modal, confirm dialogs, and the brand-new card drag-and-drop) via real browser interaction and screenshots, and produce the first `05-qa/walkthrough/turn-011/` evidence this project has ever had.
2. **List DnD (FR6) and board DnD (FR9) still unbuilt** — task-009 and task-016, the remaining Must/Should drag-and-drop surface.
3. **FR6a click-based fallback (task-010)** — cheap, mitigates DnD risk, not yet built.
4. **NFR7 (persistence-failure warning) has no task at all** — should be added to `03-plan/backlog.md` (small, additive to `storage.js` + a toast/banner in `render.js`) rather than continuing to go unassigned.
5. **Automated test coverage is thin** — only the original boards-overview smoke test exists; no Playwright coverage for lists, cards, cascade deletes, the modal, confirm dialogs, or DnD. `test-writer` is due once `ui-tester` has run and confirmed the DnD surface behaves as expected (better to write regression coverage against verified behavior).
6. **Could-have features (task-012/013/014)** — correctly deprioritized to last.
7. **No `06-ship/` artifacts** — expected at this stage, not yet a gap.

## Next

**Recommendation: `ui-tester`.**

Rationale: this has been the stated next action since turn 10 and is now the single most valuable thing this project can do — 10 turns of "approved first-pass" static review with a real, interactive, now-drag-and-drop-capable UI that has never once been exercised by a human-shaped exploratory pass. The card DnD landed last turn (task-008) and has zero verification beyond code review; the modal (task-007) and confirm dialogs (task-011) are in the same position. Running `ui-tester` now, before more DnD surfaces (task-009/task-016) are built on top of an unverified task-008, is the highest-leverage way to catch real bugs (e.g., the reviewer's own flagged off-by-one risk in `computeDropIndex` for same-list downward reorders, `04-build/reviews/task-008-review.md`) while the surface is still small enough to debug cheaply.
